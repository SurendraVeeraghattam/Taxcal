import { AcademicyearPipe } from './academicyear.pipe';

describe('AcademicyearPipe', () => {
  it('create an instance', () => {
    const pipe = new AcademicyearPipe();
    expect(pipe).toBeTruthy();
  });
});
